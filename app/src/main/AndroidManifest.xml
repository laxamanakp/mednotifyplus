<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mednotifyplus"
        tools:targetApi="31">

        <!-- Activities -->
        <activity android:name=".Reminder.MainActivityReminder" />
        <activity android:name=".Reminder.AddReminderActivity" />
        <activity android:name=".Reminder.EditReminderActivity" />
        <activity android:name=".Cost.MainActivityCost" />
        <activity android:name=".Cost.MedicineDetailActivity" />
        <activity android:name=".Cost.AdminActivity" />
        <activity android:name=".Cost.LogsActivity" />
        <activity android:name=".MainMenuActivity" />

        <!-- Classic Splash Screen Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Mednotifyplus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Broadcast Receivers -->
        <receiver
            android:name=".Reminder.AlarmReceiver"
            android:exported="true" />
        <receiver
            android:name=".Reminder.StopReceiver"
            android:exported="true" />
        <receiver
            android:name=".Reminder.SnoozeReceiver"
            android:exported="true" />

    </application>
</manifest>
